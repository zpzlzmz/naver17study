import React from 'react';
import {Alert} from "@mui/material";

const Home = () => {
    return (
        <div>
            <Alert security='info'>Home에 오신것을 환영합니다 ~</Alert>
        </div>
    );
};

export default Home;