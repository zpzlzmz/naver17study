import React from 'react';
import {Alert} from "@mui/material";

const Home = () => {
    return (
        <div>
            <Alert severity="success"> Home 입니다.</Alert>
        </div>
    );
};

export default Home;